# SilentGate Evasion Report
## API: NtOpenProcess

**Generated by:** SilentGate v1.0.0
**Author:** JarDani
**Date:** 2026-08-01 00:52 UTC

---

## 1. Overview

| Field | Value |
|-------|-------|
| Target API | NtOpenProcess |
| SSN decimal | 38 |
| SSN hex | 0x26 |
| Architecture | x64 |
| Windows Build | Windows 10 22H2 simulation baseline |
| Source | simulation_mode |

---

## 2. SSN Resolution

SSN 38 resolved by parsing ntdll.dll PE export table at runtime.
Extracted from mov eax instruction at start of NtOpenProcess stub.

---

## 3. Evasion Confidence

| EDR Product | Confidence |
|-------------|------------|
| Windows Defender | 74% |
| CrowdStrike Falcon | 59% |
| SentinelOne | 63% |
| Overall | 65% |

Note: Handle acquisition patterns are visible at kernel level regardless of how the syscall is invoked

---

## 4. Remaining Detection Risks

### [HIGH] Kernel ETW callbacks
Windows kernel registers callbacks for process and thread operations. These fire at kernel level regardless of how the syscall was invoked. Indirect syscalls bypass user-mode hooks but do not silence kernel callbacks.

### [HIGH] Behavioural correlation
Advanced EDRs track sequences of API calls. If NtOpenProcess is called as part of a known injection chain (Allocate -> Write -> Protect -> CreateThread), the pattern itself triggers detection even when each individual call bypasses hooks.

### [MEDIUM] Call stack analysis
Some EDRs inspect the return address on the call stack after a syscall. If the return address points outside a known module, it flags as suspicious. Our stub mitigates this but does not fully solve call stack spoofing.

### [MEDIUM] Memory scanning
EDR products periodically scan process memory for shellcode signatures, high entropy regions, and RWX memory pages. Indirect syscalls do not affect what is written into memory.

### [LOW] Syscall instruction location
Newer EDRs monitor WHERE the syscall instruction executes from. If it executes from outside ntdll address range, it is flagged. Our gadget finder ensures execution from ntdll, but this must be verified per target.

---

## 5. Defender Perspective

### Sysmon Event ID 10
**Tool:** Sysmon
ProcessAccess events log when one process opens a handle to another. Monitor for PROCESS_ALL_ACCESS or VM_OPERATION access rights from unexpected processes.

### ETW Microsoft-Windows-Threat-Intelligence
**Tool:** ETW
The ETW TI provider fires kernel callbacks for memory allocation, write, and thread creation operations. This fires regardless of user-mode hook bypass. Subscribe to this provider in your EDR.

### Syscall origin monitoring
**Tool:** EDR kernel driver
Monitor for syscall instructions executing from memory regions outside known loaded modules. Legitimate code syscalls should always originate from ntdll.dll address range.

### Windows Event IDs: 4656, 10
**Tool:** Windows Event Log
Enable advanced audit logging for object access and process creation. These Event IDs capture the operations our APIs perform at the Windows audit layer.

### Behavioural sequence detection
**Tool:** SIEM / EDR rules
Build detection rules for the injection sequence: NtAllocateVirtualMemory -> NtWriteVirtualMemory -> NtProtectVirtualMemory -> NtCreateThreadEx targeting the same remote process within a short time window.

### Memory integrity scanning
**Tool:** EDR memory scanner
Periodically scan process memory for RWX regions, high entropy content, and shellcode byte patterns. Indirect syscalls do not prevent payload detection in memory.

---

## 6. MITRE ATT&CK Mapping

- **T1057** Process Discovery https://attack.mitre.org/techniques/T1057
- **T1055** Process Injection https://attack.mitre.org/techniques/T1055
- **T1106** Native API https://attack.mitre.org/techniques/T1106

---

## 7. Improvements Beyond v1.0

### [HIGH] Sleep obfuscation
Encrypt your payload in memory between beacon callbacks. This defeats memory scanners that run during sleep intervals.

### [HIGH] Call stack spoofing
Forge the return address on the call stack so the syscall appears to originate from a legitimate module like kernel32.dll.

### [MEDIUM] ETW patching
Patch EtwEventWrite in memory to silence ETW telemetry before sensitive operations. Removes a key detection vector.

### [MEDIUM] Handle inheritance
Use handle duplication instead of NtOpenProcess to avoid process access events that trigger on handle acquisition.

---

## Ethical Statement

Generated for authorised penetration testing only.
SilentGate is open source MIT. Security belongs to everyone.

— JarDani